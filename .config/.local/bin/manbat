#!/usr/bin/env bash
set -euo pipefail
COMMAND=$1
echo COMMAND
# :h nobuflisted (don't put it in the buflist)
# :h nofile	 will not be written; beacuse you can quit without using "!"
man "$COMMAND" | bat --language=man | nvim +"setlocal nobuflisted" +"setlocal buftype=nofile" 

